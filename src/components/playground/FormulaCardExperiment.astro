<article
  class="formula-card-experiment"
  data-formula-card-experiment
  data-active-part="none"
  aria-labelledby="formula-card-experiment-title"
>
  <div class="formula-card-experiment__field" aria-hidden="true"></div>
  <div class="formula-card-experiment__aura" aria-hidden="true"></div>

  <header class="formula-card-experiment__header">
    <div class="formula-card-experiment__title-lockup">
      <span class="formula-card-experiment__mark" aria-hidden="true">
        <span></span>
      </span>
      <div>
        <p class="formula-card-experiment__label">Прототип карточки</p>
        <h4 id="formula-card-experiment-title">Формула</h4>
      </div>
    </div>
    <p class="formula-card-experiment__help">Наведи на символ формулы</p>
  </header>

  <section class="formula-card-experiment__scene" aria-label="Интерактивная сцена формулы и графика">
    <div class="formula-card-experiment__formula-panel" aria-label="Формула равномерного движения">
      <div class="formula-card-experiment__screen-grid" aria-hidden="true"></div>
      <div class="formula-card-experiment__screen-corners" aria-hidden="true"></div>
      <p class="formula-card-experiment__mini-caption">x = x₀ + vt</p>

      <div
        class="formula-card-experiment__formula"
        aria-label="Формула: x равно x ноль плюс v умножить на t"
      >
        <button
          class="formula-card-experiment__formula-part"
          type="button"
          data-formula-part="x"
          aria-label="x — где тело окажется"
          aria-pressed="false"
        >
          x
        </button>
        <span aria-hidden="true">=</span>
        <button
          class="formula-card-experiment__formula-part"
          type="button"
          data-formula-part="x0"
          aria-label="x ноль — откуда начали"
          aria-pressed="false"
        >
          x<sub>0</sub>
        </button>
        <span aria-hidden="true">+</span>
        <span class="formula-card-experiment__term" data-fce-term>
          <button
            class="formula-card-experiment__formula-part"
            type="button"
            data-formula-part="v"
            aria-label="v — скорость"
            aria-pressed="false"
          >
            v
          </button>
          <button
            class="formula-card-experiment__formula-part"
            type="button"
            data-formula-part="t"
            aria-label="t — время"
            aria-pressed="false"
          >
            t
          </button>
          <button
            class="formula-card-experiment__term-control"
            type="button"
            data-formula-part="vt"
            aria-label="vt — прибавка координаты"
            aria-pressed="false"
          >
            vt
          </button>
        </span>
      </div>

      <p class="formula-card-experiment__explain">
        Сначала берём старт, потом добавляем путь за время t.
      </p>
    </div>

    <div class="formula-card-experiment__graph-panel" aria-label="График координаты от времени">
      <svg
        class="formula-card-experiment__graph"
        viewBox="0 0 520 330"
        role="img"
        aria-label="График x от t со стартовой точкой, текущей точкой и подсказками"
        focusable="false"
      >
        <defs>
          <marker
            id="formula-card-arrow"
            viewBox="0 0 10 10"
            refX="8"
            refY="5"
            markerWidth="7"
            markerHeight="7"
            orient="auto-start-reverse"
          >
            <path d="M 0 0 L 10 5 L 0 10 z"></path>
          </marker>
        </defs>

        <g class="formula-card-experiment__grid" aria-hidden="true">
          <line x1="82" y1="52" x2="82" y2="258"></line>
          <line x1="194" y1="52" x2="194" y2="258"></line>
          <line x1="306" y1="52" x2="306" y2="258"></line>
          <line x1="418" y1="52" x2="418" y2="258"></line>
          <line x1="82" y1="219" x2="438" y2="219"></line>
          <line x1="82" y1="174" x2="438" y2="174"></line>
          <line x1="82" y1="129" x2="438" y2="129"></line>
          <line x1="82" y1="84" x2="438" y2="84"></line>
        </g>

        <line class="formula-card-experiment__axis" x1="82" y1="258" x2="455" y2="258"></line>
        <line class="formula-card-experiment__axis" x1="82" y1="258" x2="82" y2="36"></line>

        <g class="formula-card-experiment__ticks" aria-hidden="true">
          <line x1="82" y1="253" x2="82" y2="263"></line>
          <line x1="194" y1="253" x2="194" y2="263"></line>
          <line x1="306" y1="253" x2="306" y2="263"></line>
          <line x1="77" y1="258" x2="87" y2="258"></line>
          <line x1="77" y1="219" x2="87" y2="219"></line>
          <line x1="77" y1="174" x2="87" y2="174"></line>
          <line x1="77" y1="129" x2="87" y2="129"></line>
        </g>

        <g class="formula-card-experiment__tick-labels">
          <text x="76" y="283">0</text>
          <text x="188" y="283">1</text>
          <text x="300" y="283">2</text>
          <text x="49" y="263">0</text>
          <text x="49" y="224">2</text>
          <text x="49" y="179">4</text>
          <text x="49" y="134">6</text>
          <text class="formula-card-experiment__axis-label" x="92" y="28">x, м</text>
          <text class="formula-card-experiment__axis-label" x="415" y="300">t, с</text>
        </g>

        <g data-graph-element="projection" class="formula-card-experiment__helper">
          <line x1="306" y1="129" x2="306" y2="258"></line>
          <line x1="82" y1="129" x2="306" y2="129"></line>
        </g>

        <line
          class="formula-card-experiment__start-level"
          data-graph-element="start-level"
          x1="82"
          y1="219"
          x2="438"
          y2="219"
        ></line>

        <g data-graph-element="time" class="formula-card-experiment__time-marker">
          <line x1="306" y1="258" x2="306" y2="129"></line>
          <text x="314" y="246">t</text>
        </g>

        <g data-graph-element="delta" class="formula-card-experiment__delta">
          <path d="M 328 219 C 346 219 346 129 328 129"></path>
          <line x1="320" y1="219" x2="338" y2="219"></line>
          <line x1="320" y1="129" x2="338" y2="129"></line>
        </g>
        <text
          class="formula-card-experiment__delta-label"
          data-graph-element="delta-label"
          x="352"
          y="176"
        >
          vt — прибавка
        </text>

        <path
          class="formula-card-experiment__slope-arrow"
          data-graph-element="slope-arrow"
          d="M 162 210 C 206 190 250 162 292 136"
          marker-end="url(#formula-card-arrow)"
        ></path>

        <polyline
          class="formula-card-experiment__plot"
          data-graph-element="line"
          points="82,219 306,129"
        ></polyline>
        <circle
          class="formula-card-experiment__point formula-card-experiment__point--start"
          data-graph-element="start"
          r="6"
          cx="82"
          cy="219"
        ></circle>
        <circle
          class="formula-card-experiment__point formula-card-experiment__point--current"
          data-graph-element="current"
          r="7"
          cx="306"
          cy="129"
        ></circle>
      </svg>
    </div>
  </section>

  <aside
    class="formula-card-experiment__insight"
    aria-live="polite"
    aria-atomic="true"
    aria-label="Контекстная подсказка"
  >
    <span class="formula-card-experiment__insight-line" aria-hidden="true"></span>
    <div>
      <strong data-fce-insight-title>Нажми на символ формулы — увидишь, за что он отвечает.</strong>
      <p data-fce-insight>График показывает, как меняется координата, а не дорогу по карте.</p>
    </div>
  </aside>
</article>

<script>
  import { initFormulaCardExperiment } from "../../scripts/formula-card-experiment";

  const bootFormulaCards = () => {
    document
      .querySelectorAll<HTMLElement>("[data-formula-card-experiment]")
      .forEach((card) => initFormulaCardExperiment(card));
  };

  bootFormulaCards();
  document.addEventListener("astro:page-load", bootFormulaCards);
</script>

<style>
  .formula-card-experiment {
    --card-bg: rgba(6, 12, 21, 0.96);
    --card-line: rgba(88, 216, 255, 0.3);
    --card-yellow-line: rgba(255, 216, 77, 0.42);

    position: relative;
    display: grid;
    gap: clamp(var(--pl-space-3), 1.7vw, var(--pl-space-5));
    min-width: 0;
    padding: clamp(var(--pl-space-4), 2.2vw, var(--pl-space-6));
    overflow: hidden;
    border: 1px solid var(--card-line);
    border-radius: var(--pl-radius);
    background:
      radial-gradient(circle at 18% 8%, rgba(88, 216, 255, 0.18), transparent 25rem),
      radial-gradient(circle at 84% 16%, rgba(255, 216, 77, 0.12), transparent 22rem),
      linear-gradient(180deg, rgba(255, 255, 255, 0.035), transparent 46%),
      var(--card-bg);
    box-shadow:
      inset 0 0 0 1px rgba(255, 255, 255, 0.035),
      inset 0 0 44px rgba(88, 216, 255, 0.045),
      0 18px 52px rgba(0, 0, 0, 0.34);
  }

  .formula-card-experiment::before,
  .formula-card-experiment::after {
    content: "";
    position: absolute;
    pointer-events: none;
  }

  .formula-card-experiment::before {
    inset: 10px;
    border: 1px solid rgba(88, 216, 255, 0.12);
    border-radius: var(--pl-radius);
  }

  .formula-card-experiment::after {
    top: 14px;
    right: 14px;
    left: 14px;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(88, 216, 255, 0.45), transparent);
    opacity: 0.65;
  }

  .formula-card-experiment__field,
  .formula-card-experiment__aura {
    position: absolute;
    pointer-events: none;
  }

  .formula-card-experiment__field {
    inset: 0;
    background:
      linear-gradient(rgba(88, 216, 255, 0.035) 1px, transparent 1px),
      linear-gradient(90deg, rgba(88, 216, 255, 0.035) 1px, transparent 1px);
    background-size: 42px 42px;
    mask-image: radial-gradient(circle at center, black, transparent 76%);
    opacity: 0.82;
  }

  .formula-card-experiment__aura {
    right: 9%;
    bottom: 18%;
    width: 30%;
    height: 40%;
    border: 1px solid rgba(255, 216, 77, 0.13);
    border-radius: 50%;
    transform: rotate(-10deg);
  }

  .formula-card-experiment__header,
  .formula-card-experiment__scene,
  .formula-card-experiment__insight {
    position: relative;
    z-index: 2;
  }

  .formula-card-experiment__header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: var(--pl-space-4);
  }

  .formula-card-experiment__title-lockup {
    display: flex;
    align-items: center;
    gap: var(--pl-space-3);
    min-width: 0;
  }

  .formula-card-experiment__mark {
    position: relative;
    display: grid;
    place-items: center;
    flex: 0 0 auto;
    width: 46px;
    height: 46px;
    border: 1px solid rgba(88, 216, 255, 0.38);
    border-radius: var(--pl-radius);
    background:
      radial-gradient(circle, rgba(255, 216, 77, 0.95) 0 5px, transparent 6px),
      rgba(10, 19, 31, 0.78);
  }

  .formula-card-experiment__mark::before,
  .formula-card-experiment__mark::after,
  .formula-card-experiment__mark span {
    content: "";
    position: absolute;
    width: 36px;
    height: 15px;
    border: 2px solid var(--pl-cyan);
    border-radius: 50%;
  }

  .formula-card-experiment__mark::before {
    transform: rotate(35deg);
  }

  .formula-card-experiment__mark::after {
    transform: rotate(-35deg);
  }

  .formula-card-experiment__mark span {
    border-color: var(--pl-accent);
    transform: rotate(90deg);
  }

  .formula-card-experiment__label,
  .formula-card-experiment__mini-caption {
    margin: 0;
    color: var(--pl-accent);
    font-size: 0.72rem;
    font-weight: 900;
    letter-spacing: 0;
    text-transform: uppercase;
  }

  .formula-card-experiment h4 {
    margin: var(--pl-space-1) 0 0;
    color: var(--pl-text);
    font-size: clamp(1.35rem, 2.4vw, 1.9rem);
    line-height: 1.05;
  }

  .formula-card-experiment__help {
    margin: 0;
    padding: 9px 12px;
    border: 1px solid rgba(88, 216, 255, 0.22);
    border-radius: 999px;
    color: var(--pl-text);
    background: rgba(8, 14, 24, 0.68);
    font-size: 0.88rem;
    font-weight: 750;
    line-height: 1.25;
  }

  .formula-card-experiment__scene {
    display: grid;
    grid-template-columns: minmax(260px, 0.82fr) minmax(360px, 1fr);
    gap: var(--pl-space-4);
    align-items: stretch;
    min-width: 0;
  }

  .formula-card-experiment__formula-panel,
  .formula-card-experiment__graph-panel {
    position: relative;
    min-width: 0;
    overflow: hidden;
    border: 1px solid rgba(88, 216, 255, 0.32);
    border-radius: var(--pl-radius);
    background:
      linear-gradient(180deg, rgba(88, 216, 255, 0.07), rgba(88, 216, 255, 0.018)),
      #07101b;
    box-shadow:
      inset 0 0 0 1px rgba(255, 255, 255, 0.04),
      inset 0 0 38px rgba(88, 216, 255, 0.08),
      0 0 24px rgba(88, 216, 255, 0.07);
  }

  .formula-card-experiment__formula-panel {
    display: grid;
    align-content: center;
    gap: var(--pl-space-4);
    min-height: 330px;
    padding: clamp(var(--pl-space-4), 2.2vw, var(--pl-space-6));
  }

  .formula-card-experiment__screen-grid {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(rgba(88, 216, 255, 0.075) 1px, transparent 1px),
      linear-gradient(90deg, rgba(88, 216, 255, 0.075) 1px, transparent 1px);
    background-size: 38px 38px;
    opacity: 0.76;
    pointer-events: none;
  }

  .formula-card-experiment__screen-corners::before,
  .formula-card-experiment__screen-corners::after {
    content: "";
    position: absolute;
    width: 11px;
    height: 11px;
    border-color: var(--pl-cyan);
    pointer-events: none;
  }

  .formula-card-experiment__screen-corners::before {
    top: 16px;
    left: 16px;
    border-top: 2px solid;
    border-left: 2px solid;
  }

  .formula-card-experiment__screen-corners::after {
    right: 16px;
    bottom: 16px;
    border-right: 2px solid;
    border-bottom: 2px solid;
  }

  .formula-card-experiment__mini-caption,
  .formula-card-experiment__formula,
  .formula-card-experiment__explain {
    position: relative;
    z-index: 1;
  }

  .formula-card-experiment__formula {
    display: flex;
    align-items: baseline;
    justify-content: center;
    gap: 0.22em;
    color: var(--pl-text);
    font-family: Georgia, "Times New Roman", serif;
    font-size: clamp(2.55rem, 5.8vw, 4.95rem);
    font-style: italic;
    font-weight: 700;
    line-height: 1;
    white-space: nowrap;
    text-shadow:
      0 0 10px rgba(244, 247, 251, 0.18),
      0 0 24px rgba(88, 216, 255, 0.16);
  }

  .formula-card-experiment__formula-part,
  .formula-card-experiment__term-control {
    border: 1px solid transparent;
    color: inherit;
    background: transparent;
    font: inherit;
    line-height: inherit;
    text-shadow: inherit;
    cursor: pointer;
    transition:
      border-color 0.18s ease,
      background 0.18s ease,
      color 0.18s ease,
      box-shadow 0.18s ease,
      transform 0.18s ease;
  }

  .formula-card-experiment__formula-part {
    padding: 0 0.04em;
    border-radius: var(--pl-radius-sm);
  }

  .formula-card-experiment__formula-part:hover,
  .formula-card-experiment__formula-part:focus-visible,
  .formula-card-experiment__term-control:hover,
  .formula-card-experiment__term-control:focus-visible {
    outline: 3px solid rgba(255, 216, 77, 0.55);
    outline-offset: 3px;
  }

  .formula-card-experiment__formula sub {
    font-size: 0.48em;
    line-height: 0;
    margin-left: 0.02em;
    vertical-align: -0.24em;
  }

  .formula-card-experiment__term {
    position: relative;
    display: inline-flex;
    align-items: baseline;
    gap: 0.01em;
    padding: 0 0.12em 0.03em;
    border: 1px solid transparent;
    border-radius: var(--pl-radius-sm);
    transition:
      border-color 0.18s ease,
      background 0.18s ease,
      box-shadow 0.18s ease;
  }

  .formula-card-experiment__term[data-related],
  .formula-card-experiment[data-active-part="vt"] .formula-card-experiment__term {
    border-color: rgba(255, 216, 77, 0.24);
    background: rgba(255, 216, 77, 0.045);
    box-shadow: inset 0 0 18px rgba(255, 216, 77, 0.04);
  }

  .formula-card-experiment__term-control {
    position: absolute;
    right: 0.1em;
    bottom: -1.45em;
    padding: 0.14em 0.46em;
    border-color: rgba(88, 216, 255, 0.22);
    border-radius: 999px;
    color: var(--pl-text-muted);
    background: rgba(8, 14, 24, 0.92);
    font-family: Inter, ui-sans-serif, system-ui, sans-serif;
    font-size: 0.18em;
    font-style: normal;
    font-weight: 900;
    line-height: 1.1;
    text-shadow: none;
  }

  .formula-card-experiment__formula-part[data-active],
  .formula-card-experiment__term-control[data-active] {
    border-color: rgba(255, 216, 77, 0.42);
    color: var(--pl-accent);
    background: rgba(255, 216, 77, 0.09);
    box-shadow:
      inset 0 0 18px rgba(255, 216, 77, 0.08),
      0 0 18px rgba(255, 216, 77, 0.11);
    transform: translateY(-1px);
  }

  .formula-card-experiment__explain {
    max-width: 34rem;
    margin: 0 auto;
    color: var(--pl-text);
    font-size: clamp(0.98rem, 1.45vw, 1.08rem);
    font-weight: 750;
    line-height: 1.42;
    text-align: center;
  }

  .formula-card-experiment__graph-panel {
    min-height: 330px;
    padding: var(--pl-space-3);
  }

  .formula-card-experiment__graph {
    display: block;
    width: 100%;
    height: 100%;
    min-height: 306px;
  }

  .formula-card-experiment__grid line {
    stroke: rgba(88, 216, 255, 0.085);
    stroke-width: 1;
  }

  .formula-card-experiment__axis {
    stroke: rgba(168, 190, 213, 0.48);
    stroke-width: 1.5;
  }

  .formula-card-experiment__ticks line {
    stroke: rgba(168, 190, 213, 0.34);
    stroke-width: 1.2;
  }

  .formula-card-experiment__tick-labels text {
    fill: var(--pl-text-soft);
    font-family: Inter, ui-sans-serif, system-ui, sans-serif;
    font-size: 15px;
    font-weight: 750;
  }

  .formula-card-experiment__axis-label {
    fill: var(--pl-cyan);
    font-size: 16px;
  }

  .formula-card-experiment__plot {
    fill: none;
    stroke: var(--pl-cyan);
    stroke-width: 5;
    stroke-linecap: round;
    stroke-linejoin: round;
    filter: drop-shadow(0 0 8px rgba(88, 216, 255, 0.18));
    transition:
      stroke 0.18s ease,
      stroke-width 0.18s ease,
      filter 0.18s ease;
  }

  .formula-card-experiment__point {
    stroke: rgba(8, 14, 24, 0.92);
    stroke-width: 3;
    transition:
      fill 0.18s ease,
      filter 0.18s ease,
      transform 0.18s ease;
  }

  .formula-card-experiment__point--start {
    fill: var(--pl-cyan);
  }

  .formula-card-experiment__point--current {
    fill: var(--pl-accent);
  }

  .formula-card-experiment__helper,
  .formula-card-experiment__start-level,
  .formula-card-experiment__time-marker,
  .formula-card-experiment__delta,
  .formula-card-experiment__delta-label,
  .formula-card-experiment__slope-arrow {
    opacity: 0;
    transition: opacity 0.18s ease;
  }

  .formula-card-experiment__helper line {
    stroke: rgba(255, 216, 77, 0.72);
    stroke-width: 2;
    stroke-dasharray: 7 7;
  }

  .formula-card-experiment__start-level {
    stroke: rgba(255, 216, 77, 0.72);
    stroke-width: 2.5;
    stroke-dasharray: 8 8;
  }

  .formula-card-experiment__time-marker line {
    stroke: var(--pl-accent);
    stroke-width: 3;
    stroke-dasharray: 7 7;
  }

  .formula-card-experiment__time-marker text,
  .formula-card-experiment__delta-label {
    fill: var(--pl-accent);
    font-family: Inter, ui-sans-serif, system-ui, sans-serif;
    font-size: 16px;
    font-weight: 900;
  }

  .formula-card-experiment__delta path,
  .formula-card-experiment__delta line {
    fill: none;
    stroke: var(--pl-accent);
    stroke-width: 3;
    stroke-linecap: round;
  }

  .formula-card-experiment__slope-arrow {
    fill: none;
    stroke: var(--pl-accent);
    stroke-width: 4;
    stroke-linecap: round;
    filter: drop-shadow(0 0 8px rgba(255, 216, 77, 0.24));
  }

  .formula-card-experiment__slope-arrow + path,
  marker path {
    fill: var(--pl-accent);
  }

  [data-graph-element][data-active] {
    opacity: 1;
  }

  .formula-card-experiment__plot[data-active] {
    stroke: var(--pl-accent);
    stroke-width: 6;
    filter: drop-shadow(0 0 10px rgba(255, 216, 77, 0.28));
  }

  .formula-card-experiment__point[data-active] {
    fill: var(--pl-accent);
    filter: drop-shadow(0 0 9px rgba(255, 216, 77, 0.34));
    transform: scale(1.15);
    transform-box: fill-box;
    transform-origin: center;
  }

  .formula-card-experiment__insight {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: var(--pl-space-3);
    align-items: center;
    min-width: 0;
    padding: var(--pl-space-3) var(--pl-space-4);
    border: 1px solid rgba(88, 216, 255, 0.22);
    border-radius: var(--pl-radius);
    background:
      linear-gradient(90deg, rgba(88, 216, 255, 0.07), transparent 72%),
      rgba(8, 14, 24, 0.76);
  }

  .formula-card-experiment__insight-line {
    width: 28px;
    height: 3px;
    border-radius: 999px;
    background: var(--pl-cyan);
    box-shadow: 0 0 12px rgba(88, 216, 255, 0.18);
  }

  .formula-card-experiment__insight strong {
    display: block;
    color: var(--pl-text);
    font-size: clamp(0.96rem, 1.45vw, 1.08rem);
    line-height: 1.35;
  }

  .formula-card-experiment__insight p {
    margin: var(--pl-space-1) 0 0;
    color: var(--pl-text-muted);
    font-size: 0.9rem;
    line-height: 1.42;
  }

  @media (max-width: 980px) {
    .formula-card-experiment__header {
      align-items: flex-start;
      flex-direction: column;
    }

    .formula-card-experiment__scene {
      grid-template-columns: 1fr;
    }

    .formula-card-experiment__formula-panel,
    .formula-card-experiment__graph-panel {
      min-height: 280px;
    }
  }

  @media (max-width: 600px) {
    .formula-card-experiment {
      padding: var(--pl-space-4);
    }

    .formula-card-experiment__title-lockup {
      align-items: flex-start;
    }

    .formula-card-experiment__mark {
      width: 44px;
      height: 44px;
    }

    .formula-card-experiment__mark::before,
    .formula-card-experiment__mark::after,
    .formula-card-experiment__mark span {
      width: 31px;
      height: 13px;
      border-width: 1.5px;
    }

    .formula-card-experiment__help {
      width: 100%;
      border-radius: var(--pl-radius-sm);
    }

    .formula-card-experiment__formula-panel {
      gap: var(--pl-space-3);
      min-height: 250px;
      padding: var(--pl-space-4);
    }

    .formula-card-experiment__formula {
      font-size: clamp(2rem, 10vw, 2.72rem);
      gap: 0.13em;
    }

    .formula-card-experiment__term-control {
      bottom: -1.65em;
      font-size: 0.22em;
    }

    .formula-card-experiment__graph-panel {
      min-height: 260px;
      padding: var(--pl-space-2);
    }

    .formula-card-experiment__graph {
      min-height: 252px;
    }

    .formula-card-experiment__tick-labels text {
      font-size: 16px;
    }

    .formula-card-experiment__insight {
      grid-template-columns: 1fr;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .formula-card-experiment *,
    .formula-card-experiment *::before,
    .formula-card-experiment *::after {
      transition-duration: 0.01ms !important;
      animation: none !important;
    }
  }
</style>
