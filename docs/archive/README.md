# Historical documentation

Documents in this directory are retained for provenance, prior evidence, and
rejected or superseded approaches. They are not current requirements.

For current web decisions, start with `../../AGENTS.md`, then `../current/` and
`../decisions/`.
