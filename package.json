{
  "name": "physicslab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=9.6.5"
  },
  "scripts": {
    "dev": "npm --prefix apps/web run dev --",
    "build": "npm --prefix apps/web run build",
    "preview": "npm --prefix apps/web run start --",
    "check": "npm --prefix apps/web run check",
    "test": "npm --prefix apps/web run test",
    "test:physics": "npm --prefix apps/web run test:physics",
    "test:e2e": "npm --prefix apps/web run test:e2e",
    "screenshots": "npm --prefix apps/web run screenshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^5.9.3"
  }
}
