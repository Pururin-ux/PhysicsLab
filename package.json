{
  "name": "physicslab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0",
    "npm": ">=9.6.5"
  },
  "scripts": {
    "audit:tasks": "npm --prefix apps/web run audit:tasks",
    "audit:art": "npm --prefix apps/web run audit:art",
    "dev": "npm --prefix apps/web run dev --",
    "build": "npm --prefix apps/web run build",
    "preview": "npm --prefix apps/web run start --",
    "check": "npm --prefix apps/web run check",
    "test": "npm --prefix apps/web run test",
    "test:physics": "npm --prefix apps/web run test:physics",
    "test:e2e": "npm --prefix apps/web run test:e2e",
    "test:a11y": "npm --prefix apps/web run test:a11y",
    "test:visual": "npm --prefix apps/web run test:visual",
    "verify": "npm run check && npm run test:physics",
    "verify:full": "npm run check && npm run test:physics && npm test && npm run test:e2e && npm run test:a11y && npm run test:visual && npm run build && npm run assert:routes && npm run smoke",
    "quality:lighthouse": "npm --prefix apps/web run quality:lighthouse",
    "screenshots": "npm --prefix apps/web run screenshots",
    "assert:routes": "npm --prefix apps/web run assert:routes",
    "smoke": "npm --prefix apps/web run smoke"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
